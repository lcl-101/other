const getters = {
  categoryInfo: state => state.categoryInfo,
  specialInfo: state => state.specialInfo,
  recruitScrollY:state => state.recruitScrollY,
  preImgUrl: state => state.preImgUrl,
  logoUrl: state => state.logoUrl
}

export default getters;
