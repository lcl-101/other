const getters = {
  itemList:  state => state.itemList,
  list:  state => state.list,
  pager:  state => state.pager,
  itemName:  state => state.itemName,
  recruitScrollY:  state => state.recruitScrollY,
  itemPicUrl:  state => state.itemPicUrl
}

export default getters;
